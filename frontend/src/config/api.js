const API_BASE_URL = '/api'

export const API_ENDPOINTS = {
  // Aqui você pode adicionar os endpoints da sua API
  // Exemplo:
  // PATIENTS: `${API_BASE_URL}/patients`,
  // DOCTORS: `${API_BASE_URL}/doctors`,
}

export default API_BASE_URL 